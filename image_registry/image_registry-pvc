apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-registry-pvc
  namespace: image-registry
spec:
  storageClassName: registry-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi   # match PV size, optional but recommended
